[package]
name = "xtop-layout"
version = "0.1.0"
edition = "2021"
rust-version = "1.87"
license = "MIT"
description = "Data-driven layout definitions (model, defaults, loading) for the xtop TUI system monitor"
repository = "https://github.com/xtop-cli/layouts"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
